# --- ingestao ---
pymupdf==1.24.14          # extracao coordenada do PDF fonte
pandas==2.2.3
pyarrow==18.1.0           # parquet

# --- qualidade de dados ---
pandera==0.22.1           # contratos de schema declarativos
ydata-profiling==4.12.1   # relatorio de perfilamento automatico

# --- analise / modelagem ---
numpy==2.1.3
scipy==1.14.1
scikit-learn==1.6.0
imbalanced-learn==0.13.0  # desbalanceamento da classe pre-diabetes
statsmodels==0.14.4       # regressao logistica com inferencia (p-valor, IC, OR)
xgboost==2.1.3
lightgbm==4.5.0
optuna==4.1.0             # busca de hiperparametros

# --- interpretabilidade ---
shap==0.46.0
dice-ml==0.11             # contrafactuais
fairlearn==0.12.0         # auditoria de vies por sexo/renda/escolaridade

# --- causalidade (diferencial do trabalho) ---
dowhy==0.12
econml==0.15.1

# --- visualizacao ---
matplotlib==3.9.3
seaborn==0.13.2
plotly==5.24.1

# --- rastreabilidade ---
mlflow==2.19.0

# --- dev ---
jupyterlab==4.3.4
pytest==8.3.4
ruff==0.8.4
